---
phase: 06-bento-grid-layouts
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/components/trending/TrendingBentoHero.tsx
  - src/components/dinner-time/DinnerTimeBentoHero.tsx
  - src/components/free-movies/FreeMoviesBentoHero.tsx
  - src/pages/TrendingPage.tsx
  - src/pages/DinnerTimePage.tsx
  - src/pages/FreeMoviesPage.tsx
autonomous: true
requirements:
  - BENT-03

must_haves:
  truths:
    - "Trending page has a compact bento hero section above existing movie grid"
    - "Dinner Time page has a compact bento hero section above existing service selector"
    - "Free Movies page has a compact bento hero section above existing movie list"
    - "Existing page content remains intact below the bento hero sections"
    - "Bento hero sections are responsive: single column mobile, grid layout desktop"
    - "Bento hero cells have hover effects and stagger animation on scroll entry"
  artifacts:
    - path: "src/components/trending/TrendingBentoHero.tsx"
      provides: "Compact 3-cell bento hero for Trending page"
      min_lines: 40
    - path: "src/components/dinner-time/DinnerTimeBentoHero.tsx"
      provides: "Compact 3-cell bento hero for Dinner Time page"
      min_lines: 40
    - path: "src/components/free-movies/FreeMoviesBentoHero.tsx"
      provides: "Compact 2-3 cell bento hero for Free Movies page"
      min_lines: 40
  key_links:
    - from: "src/pages/TrendingPage.tsx"
      to: "src/components/trending/TrendingBentoHero.tsx"
      via: "Import and render above existing TrendingPageComponent"
      pattern: "TrendingBentoHero"
    - from: "src/components/trending/TrendingBentoHero.tsx"
      to: "src/components/bento/BentoGrid.tsx"
      via: "BentoGrid import for layout"
      pattern: "BentoGrid"
    - from: "src/pages/DinnerTimePage.tsx"
      to: "src/components/dinner-time/DinnerTimeBentoHero.tsx"
      via: "Import and render above existing DinnerTimePageComponent"
      pattern: "DinnerTimeBentoHero"
    - from: "src/pages/FreeMoviesPage.tsx"
      to: "src/components/free-movies/FreeMoviesBentoHero.tsx"
      via: "Import and render above existing FreeMoviesPageComponent"
      pattern: "FreeMoviesBentoHero"
---

<objective>
Add compact bento hero sections above existing content on the Trending, Dinner Time, and Free Movies pages.

Purpose: Per user decision, bento sections appear within existing feature pages as additive hero sections — not replacements. Each page gets a compact 2-3 cell bento grid that highlights the page's value proposition with live data snippets and a CTA, while the existing page functionality remains intact below. These hero sections enhance the visual impact and create consistency with the HomePage bento grid language.

Output: 3 bento hero components (`TrendingBentoHero`, `DinnerTimeBentoHero`, `FreeMoviesBentoHero`), 3 updated page route wrappers.
</objective>

<execution_context>
@/Users/kohulanrajan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kohulanrajan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-bento-grid-layouts/06-RESEARCH.md
@.planning/phases/06-bento-grid-layouts/06-01-SUMMARY.md

Key codebase references:
@src/components/bento/BentoGrid.tsx — BentoGrid container (from Plan 01)
@src/components/bento/BentoCell.tsx — BentoCell with materials/hover/tap (from Plan 01)
@src/components/animation/StaggerContainer.tsx — Stagger animation wrapper
@src/pages/TrendingPage.tsx — Current Trending route wrapper
@src/pages/DinnerTimePage.tsx — Current Dinner Time route wrapper
@src/pages/FreeMoviesPage.tsx — Current Free Movies route wrapper
@src/components/trending/TrendingPage.tsx — Existing Trending page component (for understanding existing structure)
@src/components/dinner-time/DinnerTimePage.tsx — Existing Dinner Time page component
@src/components/dinner-time/ServiceBranding.tsx — Service branding data for DinnerTime context
@src/components/free-movies/FreeMoviesPage.tsx — Existing Free Movies page component
@src/hooks/useTrending.ts — Trending movies data hook
@src/hooks/useFreeMovies.ts — Free movies data hook
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create per-page bento hero components</name>
  <files>
    src/components/trending/TrendingBentoHero.tsx
    src/components/dinner-time/DinnerTimeBentoHero.tsx
    src/components/free-movies/FreeMoviesBentoHero.tsx
  </files>
  <action>
Create 3 compact bento hero sections, each as a self-contained component that uses BentoGrid (with `columns={6}` for a smaller grid) and BentoCell for layout. These are ADDITIVE — placed above existing page content.

**1. TrendingBentoHero.tsx** (in `src/components/trending/`)
Compact 3-cell layout using BentoGrid with `columns={6}`:
- **Cell 1 — Highlight cell** (col-span 3, row-span 1, glass material):
  - Use `useTrending()` to get first trending movie
  - Show movie backdrop as background with glassmorphism overlay
  - Movie title + "Now Playing" badge
  - Rating number (vote_average) displayed prominently
- **Cell 2 — Stats cell** (col-span 3, row-span 1, clay material):
  - Show "Now Playing" heading
  - Movie count from trending list: "{N} movies in theaters"
  - TrendingUp icon from lucide-react
  - Small "Updated every 30 min" note
- **Cell 3 — CTA cell** (col-span 6, row-span 1, clay material) — OPTIONAL, span full width:
  - "Scroll down to browse all trending movies" directional prompt
  - Or omit this cell if the hero looks clean with just 2 cells

Wrap in StaggerContainer with stagger=0.12 for scroll entry animation.
Add a bottom margin (`mb-6`) to separate from existing content below.

**2. DinnerTimeBentoHero.tsx** (in `src/components/dinner-time/`)
Compact 3-cell layout using BentoGrid with `columns={6}`:
- **Cell 1 — Feature intro** (col-span 3, row-span 1, glass material):
  - UtensilsCrossed icon
  - "Family Movie Night" heading
  - "Pick a streaming service, get a family-friendly pick" subtext
  - Warm gradient or food-themed background color
- **Cell 2 — Service logos** (col-span 3, row-span 1, clay material):
  - Show 4-6 streaming service logos available for dinner time
  - Import service data from ServiceBranding's `getServiceConfig()` or use inline logos
  - "Choose your service below" label
- Wrap in StaggerContainer with stagger=0.12.
- Bottom margin `mb-6`.

**3. FreeMoviesBentoHero.tsx** (in `src/components/free-movies/`)
Compact 2-3 cell layout using BentoGrid with `columns={6}`:
- **Cell 1 — YouTube branding** (col-span 3, row-span 1, glass material):
  - YouTube-themed background (red gradient or YouTube play icon)
  - "Free on YouTube" heading
  - Film icon from lucide-react
- **Cell 2 — Movie count stat** (col-span 3, row-span 1, clay material):
  - "1,000+" large number
  - "Free movies available" subtext
  - "No subscription needed" small note
- Wrap in StaggerContainer with stagger=0.12.
- Bottom margin `mb-6`.

**Shared patterns:**
- All 3 components use `BentoGrid` with `columns={6}` (compact grid, not full 12-column)
- All cells use `BentoCell` with appropriate material props
- All wrapped in `StaggerContainer` + `StaggerItem` for scroll entry stagger
- Keep hero sections compact — maximum 2 rows on desktop, so they don't push existing content too far down
- Use responsive BentoCell `colSpan` for tablet/mobile collapse
  </action>
  <verify>
`npx tsc --noEmit` — all 3 hero components compile. Each imports BentoGrid, BentoCell, StaggerContainer, StaggerItem. Each is self-contained with no side effects on existing page components.
  </verify>
  <done>
3 compact bento hero components created: TrendingBentoHero (highlight cell + stats), DinnerTimeBentoHero (feature intro + service logos), FreeMoviesBentoHero (YouTube branding + movie count). All use BentoGrid(columns=6), BentoCell with appropriate materials, and StaggerContainer for scroll animation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate bento heroes into page route wrappers</name>
  <files>
    src/pages/TrendingPage.tsx
    src/pages/DinnerTimePage.tsx
    src/pages/FreeMoviesPage.tsx
  </files>
  <action>
Update each page route wrapper to render the bento hero above existing content. These are additive changes — existing page components remain UNCHANGED.

**1. Update `src/pages/TrendingPage.tsx`:**
```typescript
import { TrendingBentoHero } from '@/components/trending/TrendingBentoHero';
import { TrendingPage as TrendingPageComponent } from '@/components/trending/TrendingPage';

export default function TrendingPage() {
  return (
    <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-2">
      <TrendingBentoHero />
      <TrendingPageComponent />
    </div>
  );
}
```

**2. Update `src/pages/DinnerTimePage.tsx`:**
```typescript
import { DinnerTimeBentoHero } from '@/components/dinner-time/DinnerTimeBentoHero';
import { DinnerTimePage as DinnerTimePageComponent } from '@/components/dinner-time/DinnerTimePage';

export default function DinnerTimePage() {
  return (
    <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <DinnerTimeBentoHero />
      <DinnerTimePageComponent />
    </div>
  );
}
```

Note: DinnerTimePage currently renders DinnerTimePageComponent without a wrapper div. Add the same max-w-7xl wrapper for consistency, and place the hero above. If DinnerTimePageComponent already has its own max-width container, adjust accordingly to avoid double-constraining — read the actual DinnerTimePage component first to check its layout before wrapping.

**3. Update `src/pages/FreeMoviesPage.tsx`:**
```typescript
import { FreeMoviesBentoHero } from '@/components/free-movies/FreeMoviesBentoHero';
import { FreeMoviesPage as FreeMoviesPageComponent } from '@/components/free-movies/FreeMoviesPage';

export default function FreeMoviesPage() {
  return (
    <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <FreeMoviesBentoHero />
      <FreeMoviesPageComponent />
    </div>
  );
}
```

Same note as DinnerTimePage — check if FreeMoviesPageComponent has its own container before adding an outer wrapper.

**IMPORTANT:** Read the existing inner page components (TrendingPage component, DinnerTimePage component, FreeMoviesPage component) BEFORE modifying the route wrappers to understand their existing layout containers. Avoid double-wrapping with max-width containers. If the inner component already has max-w-7xl, do NOT add another one in the route wrapper — just render the hero and component as siblings inside a Fragment or minimal wrapper.
  </action>
  <verify>
1. `npx tsc --noEmit` — no type errors
2. `npm run build` — builds successfully
3. Dev server: Trending page shows bento hero above the existing movie grid
4. Dev server: Dinner Time page shows bento hero above the service selector
5. Dev server: Free Movies page shows bento hero above the movie list
6. Existing page functionality below the hero is unchanged
7. Bento heroes animate with stagger on scroll entry
  </verify>
  <done>
All 3 feature pages (Trending, Dinner Time, Free Movies) render compact bento hero sections above their existing content. Existing page components are unmodified — heroes are additive. Layout containers don't double-wrap. Bento heroes are responsive and animate on scroll entry.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run build` succeeds
3. Trending page at `/#/trending` shows bento hero + existing movie grid
4. Dinner Time page at `/#/dinner-time` shows bento hero + existing service selector
5. Free Movies page at `/#/free-movies` shows bento hero + existing movie list
6. Each bento hero has 2-3 cells with correct materials
7. Stagger animation plays when heroes enter viewport
8. Mobile: heroes stack to single column
9. Existing page functionality (movie grids, service selector, YouTube links) all still work
10. No visual regression in existing page content below heroes
</verification>

<success_criteria>
- Each feature page has a visually impactful bento hero section
- Existing page content is untouched and works identically
- Heroes are compact (2 rows max desktop) and don't push content too far
- Responsive behavior matches: single column mobile, grid desktop
- Stagger reveal animations play on scroll entry
</success_criteria>

<output>
After completion, create `.planning/phases/06-bento-grid-layouts/06-03-SUMMARY.md`
</output>
