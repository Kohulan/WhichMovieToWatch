// OnboardingWizard stub — full implementation in 03-05-PLAN.md (Dinner Time / Onboarding)

interface OnboardingWizardProps {
  isOpen: boolean;
  onComplete: () => void;
}

/**
 * OnboardingWizard — Stub placeholder pending 03-05 implementation.
 *
 * Full implementation (genre/provider selection flow) is deferred to Plan 03-05.
 * Returns null until then — DiscoverPage renders normally without onboarding UI.
 */
export function OnboardingWizard({ isOpen: _, onComplete: __ }: OnboardingWizardProps) {
  return null;
}
